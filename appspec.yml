version: 0.0
Resources:
  - myLambdaFunction:
      Type: AWS::Lambda::Function
      Properties:
        Name: ALSCalendarParser
        Alias: LIVE
        CurrentVersion: "$CURRENT_VERSION"
        TargetVersion: "$LATEST" 